spring:
  thymeleaf:
    mode: HTML
    cache: false
  profiles:
    active: dev
#mybatis 分页插件
# 分页配置

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
